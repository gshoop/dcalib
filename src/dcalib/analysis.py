"""Per-anode, per-board and whole-system analysis driver.

Planned API (plan section 7): ``AnodeResult`` (one CSV row), ``analyze_anode``,
``analyze_board`` and ``analyze_all`` (spawn pool, one task per board).
Implemented in phase 4.
"""
